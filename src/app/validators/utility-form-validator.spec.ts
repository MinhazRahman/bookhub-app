import { UtilityFormValidator } from './utility-form-validator';

describe('UtilityFormValidator', () => {
  it('should create an instance', () => {
    expect(new UtilityFormValidator()).toBeTruthy();
  });
});
